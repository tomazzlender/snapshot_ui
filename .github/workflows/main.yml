name: Ruby

on:
  push:
    branches:
      - main

  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Ruby ${{ matrix.ruby }} / ${{ matrix.gemfile }}
    strategy:
      fail-fast: false
      matrix:
        ruby:
          - '3.3'
          - '3.4'
          - '4.0'
        gemfile:
          - Gemfile
          - gemfiles/minitest_5.gemfile
        include:
          # Core gem against Rack 2.
          - ruby: '3.3'
            gemfile: gemfiles/rack_2.gemfile
          # snapshot_ui-rails against Rails 7.2 (the default Gemfile covers the latest Rails).
          - ruby: '3.3'
            gemfile: gemfiles/rails_7.2.gemfile
    env:
      BUNDLE_GEMFILE: ${{ github.workspace }}/${{ matrix.gemfile }}

    steps:
    - uses: actions/checkout@v4
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{ matrix.ruby }}
        bundler-cache: true
    - name: Run the default task
      run: bundle exec rake
